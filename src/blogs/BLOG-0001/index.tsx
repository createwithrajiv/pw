/**
 * Custom UI for BLOG-0001 (data: src/data/blogs/BLOG-0001/).
 *
 * Defaults to the shared `BlogArticle` renderer — edit freely to customize just
 * this blog's layout. The component receives `{ blog }` (fully-typed `Blog`).
 * To start customizing, replace the re-export below with your own component:
 *
 *   import type { Blog } from '@/types';
 *   export default function Blog0001({ blog }: { blog: Blog }) { return (...); }
 */
export { default } from '../BlogArticle';
